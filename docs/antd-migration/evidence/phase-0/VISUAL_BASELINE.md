# Visual Baseline

> **BLOCKED**: Capturing accurate pixel-perfect screenshots of the UI at 1024x768, 1366x768, 1440x900, 1920x1080 requires a Playwright/Puppeteer visual runner which is unavailable in the text-based agent environment.
> **How to reproduce**: Run `npx playwright test --update-snapshots` in a dedicated UI test pipeline to capture baseline images of `/worklist`, `/report/:id`, and `/admin/users`.

- Dark-room ergonomics: BLOCKED (Requires physical dark room luminance testing).
- Keyboard workflow: BLOCKED (Requires manual UX validation with a radiologist).
