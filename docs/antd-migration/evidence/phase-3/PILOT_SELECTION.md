# Pilot Selection

- **Route Selected**: `/admin/catalogs`
- **Score**: 5/5
- **Reasoning**: This route exercises `SharedDataGrid`, `Search` filter inputs, complex forms (with `Select`, `Checkbox`, `Input`, `TextArea`), Tabs navigation, and permission checking. It originally used a side-pane form; we refactored it to use an AntD `<Modal>` to fulfill the Phase 3 "overlay" requirement. 
- **Blast Radius**: Low. Configuration dictionary only.
