import React from "react";

export function WorkspaceSearchBar() {
  return (
    <div className="flex-none px-3 py-2" aria-label="Workspace search">
      <div className="flex h-8 items-center rounded border border-vin-border bg-vin-shell px-3 text-xs italic text-vin-faint" role="status">
        Search is unavailable in the PR1 layout preview
      </div>
    </div>
  );
}
